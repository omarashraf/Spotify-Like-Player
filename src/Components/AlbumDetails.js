import React from 'react';
import axios from 'axios';

export default function AlbumDetails(props) {
	// let x = props.album.images.length > 0 ? props.album.images[0].url : 
	// 						'http://dalelyles.com/musicmp3s/no_cover.jpg';
	return(
		<div>
		</div>
	);
}

